enum ApplicationStage { manager, course, shortlisted }

enum FileTypes {
  image,
  sound,
  video,
}

enum BorderAlign {
  inside,
  center,
  outside,
}

enum MessageScaffoldType {
  success,
  error,
  warning,
  information,
}
